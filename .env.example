# Payload CMS
PAYLOAD_SECRET=your-secret-here-min-32-characters-long

# Database (Vercel Postgres)
DATABASE_URL=postgres://user:password@host:port/database

# Storage (Vercel Blob — imagens e vídeos do Payload em /admin)
# Obtenha em: Vercel → Projeto → Storage → Blob → Create → copiar token
# Defina também nas variáveis de ambiente do projeto na Vercel
BLOB_READ_WRITE_TOKEN=your-blob-token-here
