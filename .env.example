# ========== Payload CMS (obrigatório) ==========
PAYLOAD_SECRET=your-secret-here-min-32-characters-long

# ========== Database — Postgres (Supabase Cloud ou self-hosted) ==========
# Supabase Cloud: Project Settings → Database → Connection string (Session mode, porta 6543).
# Supabase self-hosted (seu servidor): use o IP e a senha do .env do servidor.
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
DATABASE_URL=postgresql://postgres:[POSTGRES_PASSWORD]@76.13.236.240:6543/postgres?sslmode=disable

# ========== Supabase Storage (S3-compatible — uploads em /admin) ==========
# Prioridade: se S3_* estiverem definidas, usa Supabase. Senão, usa Vercel Blob (se BLOB_READ_WRITE_TOKEN).
# Self-hosted: endpoint = http://76.13.236.240:8001/storage/v1/s3 (criar bucket "media" no Studio).
# Cloud: Supabase Dashboard → Project Settings → Storage → S3 Connection
# S3_ENDPOINT=http://76.13.236.240:8001/storage/v1/s3
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=media
S3_REGION=auto

# ========== Vercel Blob (fallback — só usado se S3_* não estiverem definidas) ==========
BLOB_READ_WRITE_TOKEN=

# ========== Supabase — URL e chaves (frontend + Storage público) ==========
# Self-hosted: http://76.13.236.240:8001 | Cloud: https://xxx.supabase.co
# ANON_KEY está no .env do servidor (ou no Studio → Project Settings → API).
NEXT_PUBLIC_SUPABASE_URL=http://76.13.236.240:8001
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=  (só no servidor, nunca no frontend)
