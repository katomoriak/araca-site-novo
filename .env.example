# ========== Payload CMS (obrigatório) ==========
PAYLOAD_SECRET=your-secret-here-min-32-characters-long

# ========== Database — Supabase (Postgres) ==========
# O Payload usa apenas DATABASE_URL. No Supabase: Project Settings → Database → Connection string.
# Use "Session mode" (porta 6543) / URI com pooler. Ex.: POSTGRES_PRISMA_URL ou equivalente.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true

# ========== Vercel Blob (opcional — uploads em /admin) ==========
BLOB_READ_WRITE_TOKEN=

# ========== Supabase (opcional — Auth, Realtime, etc.) ==========
# Só necessário se for usar Supabase Client no front. O Payload não usa essas chaves.
# NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
