# ========== Payload CMS (obrigatório) ==========
PAYLOAD_SECRET=your-secret-here-min-32-characters-long
# Opcional: token para POST /api/reset-admin-password (redefinir senha de usuário por email). Use só em recuperação.
# RESET_ADMIN_SECRET=seu-token-secreto-aqui

# ========== Database — Postgres (Supabase Cloud ou self-hosted) ==========
# Supabase Cloud: Project Settings → Database → Connection string (Session mode, porta 6543).
# Supabase self-hosted (seu servidor): use o IP e a senha do .env do servidor.
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true
DATABASE_URL=postgresql://postgres:[POSTGRES_PASSWORD]@76.13.236.240:6543/postgres?sslmode=disable

# ========== Cloudflare R2 / S3-compatible (uploads em /admin e storage) ==========
# R2: Cloudflare Dashboard → R2 → S3 API. Um bucket para tudo (blog/, projetos/, etc.).
# Supabase Storage também usa S3_* quando NEXT_PUBLIC_SUPABASE_URL está definida.
# Prioridade: R2 (se NEXT_PUBLIC_R2_PUBLIC_URL + S3_*) > Supabase S3 > Vercel Blob.
S3_ENDPOINT=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BUCKET=mediaaraca-dev
S3_REGION=auto

# URL pública do bucket R2 (após habilitar Public access no bucket). Obrigatória para R2.
# Ex.: https://pub-9ca9f8ba8c9d47518d53ef4b3818ed26.r2.dev
NEXT_PUBLIC_R2_PUBLIC_URL=

# ========== Vercel Blob (fallback — só usado se S3_* não estiverem definidas) ==========
BLOB_READ_WRITE_TOKEN=

# ========== Newsletter (envio e link de desinscrever) ==========
# URL base do site (para o link "Cancelar inscrição" no rodapé dos e-mails)
NEXT_PUBLIC_SITE_URL=https://seudominio.com
# SMTP (opcional; sem SMTP o envio de e-mail fica desativado, sem mock)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM_EMAIL=newsletter@seudominio.com
# SMTP_FROM_NAME=Nome do Site

# ========== Supabase — fallback quando R2 não configurado ==========
# Dois projetos: um para DEV (usar no .env.local) e um para PROD (usar no Vercel).
# ANON_KEY no Studio → Project Settings → API. Payload não usa essas chaves.
NEXT_PUBLIC_SUPABASE_URL=https://pfpv0hqb6a0kjgr0.public.blob.vercel-storage.com/FJO__VIDEOFACHADA_01_R00.mp4
# Vídeo do hero da home. Se definida, usa esta URL (ex.: Vercel Blob) e evita egress do Supabase.
# Deixe vazio para fallback: vídeo do bucket media do Supabase (FJO__VIDEOFACHADA_01_R00.mp4).
# NEXT_PUBLIC_HERO_VIDEO_URL=
# Bucket de projetos (galeria da home). Se NEXT_PUBLIC_SUPABASE_URL estiver definida, as imagens dos projetos vêm daqui.
NEXT_PUBLIC_SUPABASE_PROJETOS_BUCKET=a_public
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Chave service role (só no servidor, nunca no frontend). Uploads blog e URLs assinadas (Projetos).
# Crie o bucket "media" no Supabase Storage (público) para imagens do blog.
# SUPABASE_SERVICE_ROLE_KEY=
